package com.badlogic.gdx.net;

/**
 * TeaVM stub: desktop {@code NetJavaImpl} uses {@link java.util.concurrent.ThreadFactory}.
 * gdx-teavm replaces networking with {@code WebNet} at runtime.
 */
public class NetJavaImpl {

    public NetJavaImpl() {
    }

    public NetJavaImpl(int maxThreads) {
    }
}
